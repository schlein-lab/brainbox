# Rust/wasm build output — regenerated by `cargo build --target wasm32-unknown-unknown
# --release` + wasm-bindgen. The SHIPPED artifact is pkg/ (the wasm-bindgen glue + .wasm the
# portal serves from ~/.config/phantom-portal/static/phantom-wasm/); target/ is not tracked.
/target
