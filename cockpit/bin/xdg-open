#!/usr/bin/env bash
for a in "$@"; do
  case "$a" in http://*|https://*) exec "$HOME/.local/bin/phantom-open" "$a" ;; esac
done
for real in /usr/bin/xdg-open /bin/xdg-open; do [ -x "$real" ] && exec "$real" "$@"; done
printf '%s\n' "$*"; exit 0
